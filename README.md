# AI-UI-Generator
Deterministic AI agent that converts natural language into working React UI using a fixed component library. Implements a multi-step Planner → Generator → Explainer pipeline with safe rendering, iteration support, and version rollback.
